{
  "name": "browser-snaps",
  "version": "1.2.0",
  "private": true,
  "description": "A lightweight Chrome extension for capturing website navigation pages into PDF.",
  "author": "Lewis John Villamor",
  "license": "MIT",
  "scripts": {
    "check": "node --check src/background.js && node --check src/offscreen.js && node --check src/pdf.js && node --check src/popup.js",
    "test": "node --test tests/*.test.mjs"
  }
}
